//Pure functions.
//Funciones que retornan un dato nuevo pero no modifican los valores de las variables.
//Retornan el mismo numero de entradas.
const numero = 20;
const duplicar = n => ( n * 2 );

const resultado = duplicar(numero);
console.log(resultado);